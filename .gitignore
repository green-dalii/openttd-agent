node_modules/
dist/
coverage/
*.log
.DS_Store

# OpenTTD sandbox data dirs (runtime-created)
data/
.sandbox/
*.sav

# Scratch / throwaway scripts (CDP probes, one-off verification scripts).
# Keep them out of the repo root: use .scratch/ and delete when done.
/.scratch/
/probe*.mjs
/vfy*.mjs
