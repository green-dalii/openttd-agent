# MEMORY.md — 过程教训 + 当前方向（跨 session 记忆）

> 2026-09-12 起本文件兼作**方向与实施计划的活记忆**（项目所有者指示）：
> 教训（A–F）之外，「当前方向」一节随时更新修订，替代一次性 /docs 计划文档。

> **职责**：记录**踩过的坑与错误模式**，让下一个 session（或 compact 后的自己）不再重复。
> **禁止**：系统事实（→ `SPEC.md`/`docs/`）、可执行规范条文（→ `AGENTS.md`）、进度计划（→ `ROADMAP.md`）。
> 范围以 `AGENTS.md` §9 的职责表为唯一权威。
>
> 每条格式：**现象 → 根因 → 规则**。新教训追加到对应分类末尾。
> 与 `AGENTS.md` 的关系：本文是**复盘叙述**，AGENTS 是**可执行准则**；一条教训若沉淀为硬规则，写进 AGENTS 并在本文留指针。

---

## 0. 当前方向与下一步（活记忆；**详细计划在 `ROADMAP.md`，此处只留状态与判断**）

**业务命题**：接上 LLM 的 agent 能否**自主玩** OpenTTD，并从自己的历史中**积累可测量的进步**？

### 0.1 能力判定（2026-09-19 复核，证据来自真机日志与代码）

| 问题 | 判定 | 证据 |
|---|---|---|
| 能否无人干预跑完整局？ | **能** | 100+ 局真机、`reachedHorizon=true`、施工完成、决策/工具/账本齐全、`rc=0`（D13） |
| 能否**自主决策**（不是内置 AI 代打）？ | **能** | `meta.llm.kind=real`、`mode=agent`、`decisions 2–16/局`、`trigger` 多样（start/event/phase_change/wait_until） |
| 是否算"会玩"？ | **还不算** | 见下面三条硬约束 |

**"会玩"缺的三件（按阻塞程度）**：

1. **动作面只有一种**：GS 只暴露 7 条命令（`ping/demo/build_bus_route/add_vehicles/blueprint/status/clear`），
   其中变更型只有两条半 → agent 只能**建公交线 + 加车**。没有选货、拆线、换车、融资、
   其他运输方式。**"能不能玩"的上限由环境暴露的动作决定，不由 agent 循环决定。**
2. **目标与判据不一致**：提示词说 "profitable"，判据是 `deliveredRun`。实测 106 局里
   `income>0` 的只有 **9 局**（施工期年为负）——目标**通常不可达**，agent 会为做不到的事优化。
3. **有后果的决策太少**：300 游戏日窗口里只有 10–16 次决策（施工是单线程 FIFO，期间无事可做），
   且翻盘空间小；这也正是"记忆无处兑现"的根源（§10.43）。

**已具备的地基**（不必再做）：事实通道（模拟时钟/施工推进+ETA/线路经济/通道健康）、
归因（工具必须观测自身效果或拒绝）、测量（`deliveredRun` + 场景/窗口对齐 + 统计量）、
经验契约（观察式记录、可被推翻、拒绝回到模型）。

### 0.2 当前阶段

| 阶段 | 状态 | 事实位置 |
|---|---|---|
| R1 执行顺序 / 工具预算 / 披露路径 | ✅ | SPEC §10.75 |
| R2a 经验 = 带实测读数的观察 + 可被推翻 | ✅ | SPEC §10.76 |
| R2b 反思走 Agent + 记录工具 | ✅ | SPEC §10.77 |
| **M-PLAY（下一步，当前）**：让"LLM 自主玩一局"成为**可辩护**的主张 | 🔵 | `ROADMAP.md` M-PLAY 节 |
| 早期阶段（Phase A–C 重构、NEXT-2 决策空间） | ✅ 已完成 | SPEC §10.45–§10.63 |

**未测旋钮（不得声称已有效果）**：`sessionId` 缓存收益、`deliberation off-vs-low` 的质量-成本权衡、
反思**产出率**（同配置 1/2 局模型没调记录工具）。

### 0.3 不漂移协议（每阶段收尾必做，长期有效）

① gate 全绿 + 真机断言；② 三行对齐（完成 / 偏离 / 下阶段方向是否仍成立）；
③ SPEC·CHANGELOG·ROADMAP·MEMORY **一次性**同步；④ 开工前重读本节——
**工作若不在杀根因或给记忆出题，停下来问所有者**。

> NEXT-2 的实施方案与执行日志（2026-09-16/17 的 127 行）已完成使命，**不再保留在此**：
> 事实在 SPEC §10.53–§10.63，进程在 `ROADMAP.md` 顶部交接块；这里只留上面的结论。

## 索引：教训一句话（**查这里，不必通读下面**）

> 阅读顺序：先看本索引定位 id，再只看那一条。每条格式 **现象 → 根因 → 规则**。

**A 验证方法**（本仓库最大的坑）

| id | 一句话规则 |
|---|---|
| A1 | "没有报错"只在代码**真的执行过**时成立——别用空状态验证组件，warn 也要采 |
| A2 | 断言要指向**用户可观察的结果**，不能是代理指标（像素/异常数/进程存活） |
| A3 | 一个变体失败 ≠ 能力不可得；下否定结论前先穷举变体，并在 SPEC 写清边界 |
| A4 | 单测用 faux provider，永远抓不到"没接线"——涉及 agent/遥测必须真机验 |
| A5 | "门禁看得到、runner 收不到"：一个选项只有一个真值来源；断言行实际收到的 opts |
| A6 | 组件"沉默"时先问**世界动了吗**——用最便宜的外部量把"世界停了"与"组件停了"分开 |
| A7 | 模型只读 `summary`；断言要读**模型实际读到的文本**，不是结构体 |
| A8 | A/B 测不出差异时先问"被测者到底做了什么决策"——加样本救不了没有决策的任务 |
| A9 | 观测与控制共用通路时，改观测就是改控制（别把副作用接到数据通道上） |
| A10 | 工具必须**能观测自身效果，或明确拒绝**；"发送成功"≠"生效" |

**B 前端 / 渲染层**

| id | 一句话规则 |
|---|---|
| B1 | "静默缺失的 UI"最危险：迁移模板后逐个交互元素人工点一遍（零报错≠没坏） |
| B2 | `<template>` 不能放 `<svg>` 里（SVG 命名空间下没有 `.content`）——拼字符串 + `x-html` |
| B3 | 响应式读取必须在 effect 的**同步作用域**内完成，异步边界的读取不算依赖 |
| B4 | 永不用 `scrollIntoView` 跟随列表（会滚动 document）；用自管 `scrollTop` |
| B5 | `Intl` 是数据不是契约（compact 后缀随引擎变）；单位词表要自己拥有一份 |
| B6 | `x-for` 的 `:key` 必须唯一，且列表写入要幂等 upsert（重复投递不该产生分歧状态） |
| B7 | 页面缺一个依赖脚本 = 功能静默消失；依赖闭包要静态守卫 |
| B8 | 禁用控件必须"看起来禁用"并**说明下一步**——假可用按钮等于 bug |
| B9 | 第三方库的"可选"参数可能被静默忽略；只能靠测量可观察结果来判定 |
| B10 | 换成图表库 ≠ 语义自动正确；断言视觉结果（每种颜色都在、顺序对） |
| B11 | 框架"剧透"会删掉学习机会：文案只描述世界/契约，不替 agent 做决定 |
| B12 | 渲染验证要用原始像素与等分探针，别用会引入偏差的聚合 |

**C 数据与一致性**

| id | 一句话规则 |
|---|---|
| C1 | 同一概念只允许一处实现（两份 = 静默漂移）；另一条路径"看起来没问题"正是掩盖 bug 的原因 |
| C2 | 改动有**实测理由**支撑的设计前，先找到那条理由，再对齐或明确推翻并更新文档 |

**D 环境与工具**（D0–D3 是环境事实，D4–D32 是方法教训）

| id | 一句话规则 |
|---|---|
| D0 | 环境事实表（凭证/二进制/dataDir/清理命令）——compact 后照用 |
| D1 | 清理开发进程要用对的匹配模式（`cli/run.ts`），并复核端口 |
| D2 | subagent 不可用时**显式报告偏差**，不要静默降级成全内联 |
| D3 | 静态资源必须有 `no-cache`+`ETag`；"我这边好的"先怀疑版本不一致 |
| D4 | 加注入通道必须三问：进账本？被 `--no-memory` 关？进对照判据？ |
| D5 | 真机实验期间**不跑 gate**（端口被占 → 假红） |
| D7 | 实验自变量**分配时**记录，事后推断归类必漏（"干预没送到"的局长得像对照） |
| D8 | 继承来的"事实"必须重测；否定性结论尤其危险 |
| D9 | 暂停的代价是"世界停止推进"——瓶颈在世界本身时暂停是净亏 |
| D10 | 重尾量只报均值会给相反结论；先看分布，再选统计量 |
| D11 | 结果不可判定时**先怀疑实验设计**（窗口/零膨胀/场景差异），修设计比加样本便宜 |
| D12 | 实验进行中**冻结被测对象**（不改 `src/`），否则同一 block 内前后局不同代码 |
| D13 | 退出码不是错误码——先 grep 出谁写这个码，再决定那局废留 |
| D14 | 会重置的计数器不是测量值（当季 counter → 跨季积分 + 缺口标记） |
| D15 | 部分失效比彻底崩溃更危险：逐局计数通道健康，并印在结果旁边 |
| D16 | 修仪器 ≠ 降方差；"不可比"与"噪声大"要分开报告 |
| D17 | 决定结局的约束必须是被测者**看得见的事实**（瓶颈资源的状态） |
| D18 | 先定义"一次观测"的边界（用被测现象自己的时间单位），墙钟只是工程约束 |
| D19 | 契约改了，**边界执行**也要跟着改；声明 ≠ 保证，测量才是保证 |
| D20 | 沉默的跳过 = 让 agent 学会"白做"；被忽略的输入必须变成明确拒绝 |
| D21 | "在动"不等于"在前进"：给 agent 的必须是单调、有终点的量（剩余量/ETA） |
| D22 | 同一事实有两条通道时粒度可能差 10 倍；选之前问语义，并写进注释 |
| D23 | 有杠杆 ≠ 杠杆到处都灵：先测响应曲线，确认效应落在陡峭区间 |
| D24 | 把"运气"移出测量窗（预建场景），而不是用统计去扣除它 |
| D25 | 从哪个信号蒸馏经验，就等于在教什么——蒸馏输入必须与判据同源 |
| D26 | 守卫检查"我们包装出来的形式"时只保护包装；守卫要作用在**内容**上 |
| D27 | 先用尽手里的库，再自己造；**依赖库的默认值不是契约** |
| D28 | 披露必须挂在**产生判决的那条路径**上，并有测试证明它真取到了数 |
| D29 | 用"追加"表达删除时，合并规则必须让删除赢（含反序输入测试） |
| D30 | 启发式守卫要用真数据量召回率，并写明它只是**第二道闸** |
| D31 | 提示词与运行时是**同一份契约的两半**；不一致的失败是静默的 |
| D32 | "能不能做某件事"要沿**环境暴露了多少动作**这条链去查，而不是查 agent 循环 |
| D33 | 无法诊断的失败会被读成"没有内容"；证据要落在**不会消失的介质**里，且两种成因分开计数 |
| D34 | 源码里有这个分支 ≠ 它在**可达状态**下会被调用：读完分支还要读**调用点的守卫** |

**E 提交卫生** · **F 文档一致性**

| id | 一句话规则 |
|---|---|
| E1 | 每个 commit 就是一次收尾（文档同 commit 内同步；ROADMAP 待办当场划掉） |
| E2 | 同一次工作的收尾不要拆成多个 commit（`--amend`） |
| F1 | 同一事实只在一处权威，其余用指针；写进文档的数字当场核实 |

## A. 验证方法（本仓库最大的坑）

### A1. 「没有报错」只在代码真的执行过时才成立（2026-09-12）

**现象**：我向用户报告"三个页面 0 控制台错误"并交接。用户实测 Live 页满屏报错：
`Alpine Expression Error: ... reading 'children'` + `Uncaught ReferenceError: m is not defined`。

**根因**（三层，每一层单独都足以放过错误）：
1. 我的 E2E 跑在一次 **`stageViews` 仍为空**的会话上（agent 刚启动，还没到施工阶段），
   出错的 `<template x-for>` **从未渲染** → 不产生任何错误。
   **我用一个没有数据的状态"验证"了我刚写完的组件。**
2. 探针只监听 `Runtime.exceptionThrown` + `console.error`；Alpine 的表达式错误走
   **`console.warn`** → **被我的过滤器丢掉**。
3. 我把"采集到 0 条错误日志"当成了"页面没问题"这个结论的证据。

**规则**：见 `AGENTS.md` §5.2。要点：验证前先问「这段代码这次真的跑了吗」；
采集全部日志级别；断言用户能观察到的东西。

### A2. 验证代理指标 ≠ 验证结果（重复犯错，本仓库至少 3 次）

**现象/实例**：
- **uPlot 图表宿主误用 `<canvas>`**：注入的 DOM 成了 canvas 的 fallback content（浏览器
  永不渲染），且画布被拉伸到 1108px。用户看到两个空的大黑框。而我当时的"验证"测的是
  **canvas 里的像素**，并报告"230 painted pixels"——**像素在一个没人渲染的画布里**。
- 迁移后报告"0 控制台错误"，实际见 A1。
- 阶段 2 我检查 `el.querySelector('canvas')` 是否存在，即断言"我注入的东西在"，
  而不是"用户看得见"。

**根因**：选择断言时挑了**容易测的量**（像素数、异常数、进程存活），而不是**用户能看到的量**
（元素可见性、DOM 文本、wrapper 的实际屏幕盒子）。

**规则**：断言必须指向**用户可观察的结果**。UI 断言至少要有
`offsetParent !== null` 或 `getBoundingClientRect()` 的非零尺寸，
不能只断言"我插入的节点存在"或"有像素"。

### A3. 「某个变体失败」≠「整个能力不可得」（2026-09-12）

**现象**：`SPEC.md` 曾把"headless 无法截图"写成一条系统事实，据此放弃了阶段画面。
实际上只有 viewport 变体失败，minimap 变体是可行的。

> 事实本身（哪个变体可行、为什么）**归 `SPEC.md` §10.2**，本文不重述——
> 本文只记录**我的推理错误**：从一个变体失败推广成"整个能力不可得"，
> 并把推广结论固化成了系统事实。

**根因**：试一个变体失败后做了**过度推广**，并把推广结论固化成 SPEC 事实。

**规则**：下"不可得/不支持"这类否定结论前，**穷举变体**（子命令、参数、模式），
并在 SPEC 里写清**边界到底在哪**（"对 X 不成立，对 Y 成立"），不要写笼统的"不可得"。

### A4. 单测用 faux provider，永远抓不到"没接线"

**现象**：v0.3~v0.5 期间 `--agent` 的 E2E 只验证了"进程起来了、有 token 计数"。
真实情况是三个 Session 全是 `mode=watch` + `kind=faux` + `decisions=0` ——
**内置 CPU AI 在打**，而面板一切正常。单测全绿。

**根因**：faux provider 让"没接线"也能产出看似正常的遥测。

**规则**：见 `AGENTS.md` §5.1 的 8 条必证断言。涉及 agent/决策/遥测的改动必须跑 `test:live`。

### A10. 一个永远说"成功"的工具会让 agent 丧失学习能力（2026-09-12，用户 e2e）

**现象**：用户 e2e 日志里，模型连续调用 `add_vehicles` 四次，
每次都 `ok=true`，而 `vehicles` 始终为 0。它只能无限重试同一个动作。

**根因（两层）**：
1. **工具说谎**：`add_vehicles` 无条件 `ok=true` —— 它只表示"我把命令写进了 socket"，
   既不等于标牌被读到，也不等于车队变了。这与 `build_bus_route` 不同（那条命令确实
   启动了异步施工，所以 `ok=true` 有实际含义）。
2. **命令确实不可能生效**：执行器靠**克隆头车**扩容，而头车由 `PhaseBus()` 买；
   车队为 0 时没有可克隆对象。且执行器只在 `stage=="done"` 时才读那个请求标牌，
   施工未完成时标牌永远不会被读。

**规则**：**工具必须能观察到自己的效果，或者明确拒绝。**
"发送成功"与"生效"是两件事，报成同一件会让模型学不到任何东西——
它会把"没有变化"归因为"世界没配合"，而不是"这个动作做不到"。
判据：**如果这个动作在当前状态下不可能生效，工具应当拒绝并说明原因与下一步。**

（相关：`placed:1` 这种含糊字段名助长了误读，已改为 `signPlaced`。）

---

## B. 前端 / 渲染层

### B1. 「静默缺失的 UI」是最危险的类别（2026-09-12，一次迁移踩中 3 次）

**现象**（迁 Alpine 期间，三个都是**零报错**）：
1. `companyCards()` 被模板 `x-for` 引用，但**视图模型里从未实现** → ReferenceError（这个至少报错了）
2. Providers 页的 **Built-in / Custom 模式开关被我整段删掉** → 渲染成一个空 `<div>`，
   无任何异常。**用户切换模式的唯一入口消失了。**
3. `$refs.provBox` / `$refs.modelBox` 取了 ref，但 HTML 上**没写 `x-ref`**
   → 两个搜索下拉**从未挂载**（不报错，只是没出现）。

**根因**：重写声明式模板时，"元素还在不在、指令接没接上"没有任何自动检查。
单测/类型检查/eslint 都看不见"运行时绑定缺失"。

**规则**：迁移或重写动态模板后，**逐个交互元素人工点一遍**（开关、下拉、按钮、折叠），
并断言 DOM 有内容。见 `AGENTS.md` §5.2 第 4 条。

### B2. `<template>` **不能放在 `<svg>` 内部**（2026-09-12，我两次误判才找到真因）

**现象**：Live 页满屏
`Alpine Expression Error: Cannot read properties of undefined (reading 'children')`
以及一连串 `Uncaught ReferenceError: m is not defined`（`m.x1`、`m.y` …）。

**我的第一次误判**：我以为根因是「`x-for` 内放了两个兄弟 `<template x-if>`，违反了
「模板内恰好一个根元素」。改成两个平铺 `x-for` 后 **错误依旧**。

**真因**（Chrome 实测证据）：
```js
{ expr: 'm in stageRoutes(v)', ns: 'SVG', isHTMLTemplate: false, contentDefined: false }
```
`<template>` 在 `<svg>` 内部被 HTML 解析器当作 **SVG 命名空间元素**，而不是
`HTMLTemplateElement`。它的 `.content` 是 `undefined`，于是 Alpine 的 `x-for` 读
`.content.children` 直接抛错，循环变量也永远不会绑定。

**规则**：需要在 SVG 里画动态内容时，**把 SVG 标记拼成字符串，用 `x-html` 注入**
（注入时处于 HTML 解析上下文，`<svg>` 命名空间才正确），生成逻辑做成纯函数以便单测。
已在静态守卫里锁住：`lintTemplatesInsideSvg()`。

**教训**：报错信息里的**元素上下文**（` line.ov-route`）是关键线索——它告诉我 Alpine
正在处理 SVG 里的节点。我第一次只看了表达式（`stageMarks(v)`）就下结论，
把一个**解析层**问题当成了**模板结构**问题。

### B3. 响应式读取必须在 effect 的**同步作用域**内（2026-09-12）

**现象**：Sessions 页的 donut 图**永远不绘制**——canvas 在、legend 正常、无报错，只是全白。

**根因**：`drawDonut()` 在 `$nextTick` 回调里读 `tokenSlices()`。Alpine 的依赖追踪发生在
同步求值期间，跨过异步边界后读取**不算依赖** → `x-effect` 永不重跑。

**规则**：在 `x-effect` 里，**先同步读完所有要用的响应式数据**，再进入异步/`$nextTick`。
已加回归测试：断言读取发生在异步边界之前。

### B4. 永不用 `scrollIntoView` 跟随列表（2026-09-12，用户报告）

**现象**：Agent steps 更新时**整个页面自己滚动**，用户正在看上面内容时被拽走。

**根因**：`scrollIntoView({block:"nearest"})` 会滚动**所有可滚动祖先，包括 document**。
实测：列表 520/2121px 时它把页面滚了 692px。

**规则**：用 `UI.scrollToEnd(el)`（设自己的 `scrollTop`）或 `UI.keepVisible(container, child)`
（基于 rect 计算）。已加**静态护栏**：页面脚本里出现 `.scrollIntoView(` 即测试失败。

### B5. `Intl` 是数据，不是契约（2026-09-11）

**现象**：`Intl.NumberFormat(..., {notation:"compact"})` 的后缀拼法在不同引擎不一致：
Node 给 `1.5B`，Chrome 给 **`1.5bn`**，而我们手写的图表给 `1.5B` → **同一页 KPI 与图表
对同一个数字显示不同单位**。

**根因**：compact 后缀拼写来自 **CLDR 数据**，随引擎/版本变化，不构成可依赖的契约。

**规则**：`Intl` 只负责**数字本身**；单位阶梯（k/M/B/T）是**我们自己的词表**
（`COMPACT_UNITS`），KPI 与图表必须共用同一份实现。已加两个测试锁定：
① 一个拒绝 compact 的"敌对 Intl"；② `fmtTok(v) === charts.util.fmtCompact(v)`。

### B6. Alpine `x-for` 的 `:key` 必须唯一，否则**整段列表渲染为空**（2026-09-12）

**现象**：`stageViews` 里确实有 2 条数据，但 `.stage-snaps` **一个节点都没渲染**，
且**无任何控制台报错**。

**根因**：两条记录的 `index` 都是 `0`（同一帧被投递了两次），而 `index` 是 `x-for` 的 `:key`。
**重复 key 让 Alpine 渲染出 0 个节点**，而不是 2 个或 1 个。

**规则**：`:key` 必须唯一。更重要的是，**列表写入要幂等（upsert by 稳定 id），不要盲 append**
——重复投递不应该产生分歧状态。已加测试：重复投递同一帧后长度不变、
`new Set(keys).size === list.length`。

### B7. 页面缺一个依赖脚本 = 功能静默消失（2026-09-12，第 3 次同类错误）

**现象**：新建的 Evolution 页图表**永远不出现**——div 存在、可见、1278×120、ref 正确解析、
`Charts.line` 也在，但 `innerHTML` 是空的，**零控制台输出**。

**根因**：`evolution.html` 漏了 `<script src=".../uplot/uPlot.iife.min.js">`。
`ucharts.js` 的 `mount()` 第一行就是 `if (!uplotAvailable()) return null;`
—— 静默返回。同一页面加载了 `ucharts.js`，却没加载它依赖的全局。

**为什么这是"第 3 次"**：同类错误本仓库已踩过三次（uPlot 宿主误用 `<canvas>`、
Providers 的 `x-ref` 缺失、本次缺 vendor 脚本）——共同点是**功能没了，但没有任何异常**。

**规则**：页面脚本之间的**依赖闭包**必须被静态守卫，不能靠人记得。
已加入 `test/unit/web-assets.test.ts` 的 `script dependency closure`：
加载 `ucharts.js` 的页面必须同时加载 uPlot bundle 与样式；加载 `charts.js` 必须加载
`common.js`；加载 Alpine 必须加载 bridge。

**更普遍的教训**：`if (!dependency) return null;` 这种**静默降级**在页面里是危险的——
它把"配置错误"伪装成"这里本来就没东西"。若必须降级，至少 `console.error` 一次
（`ucharts.js` 的 `hostCannotRenderChildren` 就是这么做的，所以那类错误当场就被发现了）。

### B8. 一个禁用的按钮，如果看起来可点且不解释 = bug（2026-09-12，用户报告）

**现象**：用户在 Providers 页改了 Provider 与模型，点 **Save 毫无反应**；刷新后配置退回原样。
用户只能得出"保存坏了"的结论。

**实测复现**：选中 Provider 但**没选模型**时 `saveReady()` 为假，按钮 `disabled`。
而 CSS **没有给 `:disabled` 任何样式**，于是它渲染成 `opacity:1; cursor:pointer`
——**和可用状态一模一样**。点下去：不发请求、不报错、不提示，`msg === ""`。

**根因有两层**：
1. 换 Provider 会清空模型（`selectProvider` 把 `this.model = ""`），用户必须**再选一次模型**；
   不选就落到"静默禁用"状态。
2. 禁用状态**没有任何可见性**（既不变灰，也不说明原因）。

**规则**：
- 禁用控件必须**看起来**禁用（`opacity` + `cursor:not-allowed`）；
- 禁用时必须**说明下一步做什么**（`saveHint()` 按分支给出"Pick a provider…" /
  "Pick a model — the provider changed…"），**不能只是一个假的可用按钮**；
- 断言 `saveReady() === false` 是断言一个**谓词**，不是断言一次**体验**——
  两者之间正是漏测的地方（见 B1）。

**附带发现的真 bug**：切到 Custom 模式时**继承了目录 Provider 的 id**，
把 `{"source":"custom","providerId":"deepseek"}` 写进了 `llm.json`——一条把凭据归错主的混合记录。
现在两个模式的选择分开保存（`lastCatalogProvider` / `lastCatalogModel`）。

### B9. 上游库的"可选"参数可能是**静默忽略**的（2026-09-12）

**现象**：按 uPlot 文档给 `paths.bars` 传 `disp.y0` 做堆叠，**完全没有效果**，
也没有任何报错。

**根因**：uPlot 1.6.32 的实现是
```js
let {y0: t, y1: n} = disp; null != t && null != n && ( ... )
```
—— **必须同时给 `y0` 和 `y1`**，只给一个会被静默跳过。

**规则**：接第三方库时，"我按文档传了参数"不等于"参数被使用了"。
判定方式只有一种：**测量可观察的结果**（这里是逐系列像素与纵向颜色序列），
而不是检查自己传了什么。已验证正确的输出：
`{Input:4922, Output:949, Reasoning:385}` + 自上而下 `Reasoning → Output → Input`。

### B10. 手写图表换成库，不等于语义自动正确（2026-09-12）

**现象**：v0.5.0 把 token 图从折线改成"堆叠柱"，移植时把数据**累积**了，
以为累积就等于堆叠。实际上 `paths.bars` 从零基线画，累积只导致后者盖住前者 ——
图上每个 x 只剩一个颜色，而"看起来像一根柱子"，所以**验收时没发现**。

**规则**：把图表的**数据整形**换掉时，必须断言**视觉结果**（每种颜色都出现、
且纵向顺序正确），不能只断言数据数组正确。数据对 ≠ 画对。

### B11. 框架"剧透"会删掉 agent 的学习机会（2026-09-12，用户指出）

**现象**：用户明确要求"不要把经验、教训内化到 Harness 中"。
审计发现 `SYSTEM_PROMPT` 里写着：

- "Do NOT issue the same command repeatedly while it is still building"
- "Prefer one solid route over many half-built ones"
- "add vehicles when queues grow"

而我上一轮给 `add_vehicles` 写的拒绝信息也是：
"Use observe() to watch for vehicles > 0, and only then scale the fleet."

**根因**：写这些句子时"感觉很有帮助"——它们读起来像好的文档。但它们是**策略**，
是 agent 本该从观察里自己得出来的东西。**被剧透的 agent 不会去试错，
也就没有可学的教训**——这与"让 agent 自主探索和学习"直接冲突。

**边界怎么划（已加机械守卫）**：

| | 属于 Harness？ |
|---|---|
| 工具/子系统的**契约与前提**（"本工具靠克隆头车扩容"） | ✅ 像函数签名，必须知道 |
| **世界事实与因果**（"施工是异步的"、"贷款产生利息"） | ✅ SPEC §4.2 要求框架给 |
| **交互协议**（输出 JSON 的键、何时让出回合） | ✅ 框架拥有协议 |
| **策略**（"你应该…"、"优先…"、"队列长了就加车"） | ❌ agent 自己学 |

**规则**：写 prompt/工具文案时问一句 —— **"这句话是在描述世界，还是在替它做决定？"**
后者一律删掉。守卫见 `test/unit/agent-runtime.test.ts`。

### A5. 「门禁看得到、runner 收不到」——同一 bug 出现了三次（2026-09-12）

**现象**：`--serve --offline-demo` / `--agent --offline-demo` 都会打印
"running the explicit offline demo"，然后**立刻抛 "no LLM configured"**。

**根因**：`offlineDemo` **preflight 直接读 `args`**（所以门禁放行），
但两条启动路径的 `runOpts` / `runAgent(...)` 都是**手写的对象字面量**，谁都没转发它。

三个实例：
1. `serve.ts` 的 `runOpts` 只放了 `web` / `control`；
2. `cli/run.ts` 的 `--serve` 分支手写 `runOptions`；
3. `cli/run.ts` 的 `--agent` 分支同样漏掉。

**为什么危险**：这比"没有这个 flag"更糟 —— **它谎报了将要发生的事**。
而且任何"断言 preflight 读到了 offlineDemo"的测试都会通过，因为它测的正是没坏的那一半。

**规则**：
- 一个选项被**谁**决定、被**谁**消费，必须只有一个真值来源；
  门禁与 runner 必须读**同一个已解析的值**（`resolveRunOptions`）。
- 测试的断言必须落在 **launcher 实际收到的 opts** 上，不能落在"门禁认识这个字段"上。
  "断言一个谓词，不等于断言一次体验"（A1 的同一形状）。
- 手写 `runOpts` 字面量本身就是气味：新增选项时它不会报错，只会静静丢掉。

---

## C. 数据与一致性

### C1. 同一概念重复实现会**静默漂移**（2026-09-12）

**现象**：`toWireSnapshot` 存在**两份**（`src/agent/runner.ts` + `src/game/runner.ts`），
且已经漂移——agent 那份漏了 `history` 字段。后果：**主模式（`--agent` / `--serve`）的
现金曲线一直是空的**，而 `--watch` 看起来完全正常。

**根因**：复制粘贴后各自演化；没有测试比对两份实现；两条模式路径的测试没有交叉。

**规则**：同一概念只允许**一处**实现（`src/game/wire-snapshot.ts`）。发现两份立刻合一，
并补一个测试锁定统一行为。**"另一条路径看起来没问题"不是借口——正是它掩盖了这条路径的 bug。**

### C2. 不要为"曾用实测支撑过的决定"静默改设计（2026-09-12）

**现象**：`ROADMAP` 待办要求"Token usage 图从柱图改折线"，但 v0.5.0 **刚刚**把它从折线
改成堆叠柱，理由是 token 是**构成**语义、且 input 比 output 大一个数量级会让多条折线挤成 3px
（写在 `docs/DASHBOARD-UI.md` §6c）。

**根因**：（潜在）把新需求当成对一个空白页面的改动，忽略既有决策。

**规则**：改动一个**有实测理由支撑**的设计前，先找到那条理由（读文档），
然后二选一：**对齐它**，或**明确推翻并更新文档**。禁止静默覆盖。

### B12. （已并入 B9/A2）度量本身可能是错的

**一句话**：像素统计的聚合方式（跳过透明行再算跨度）会把 1px 描边说成 49px 的带，
**差点得出"已经修好了"的错误结论**。规则见 B9（只有测量可观察结果才算验证）
与 A2（断言要指向用户可观察的结果）：渲染断言用**原始像素（含 alpha）**，
并用**等分数据**做探针——每段高度应当相等，一眼就能看出"只有一条"还是"三条都在"。

### A6. 先找"沉默"的原因，再猜"行为"的原因（2026-09-12，代价：两天）

**现象**：连续多轮把"执行器卡住"当成执行器的问题去修（查标牌可见性、标牌协议、铺路方向、
pathfinder），全部无效。真因在**观测者**那一侧：当时没有任何地方量过**游戏日历**，
于是"世界停了"与"组件停了"在观测上无法区分。

> 当时写下的 rcon 机制解释**是错的**（2026-09-17 实测：pause/unpause 双向都有效，SPEC §10.59）。
> 保留本节只为留下"一个错误归因如何挡住正确设计五天"的原始记录。

**规则**：出现"某组件长时间不动"时，**先问：它周围的世界动了吗？**用最便宜的外部观测量
（时钟、计数器）把两者分开。**先修观测，再修行为**——本项目修好心跳后，同一问题几轮就定位了。

### A7. 一个"看着有用"的字段，可能让模型完全看不到数据（2026-09-12）

`toResult()` 只把 `ActionResult.summary` 作为**模型可见文本**发出去；
`data` 进了 `details`，**模型看不到**。

我加了城镇数据后，`data.towns` 齐全，但 `summary` 里没有 → 模型仍然看不到任何选项。
**自测只查 `data` 会得出"已经给到了"的假结论。**

**规则**：断言必须读**模型/用户实际读到的文本**（`content[0].text`），
不是读结构体。同类陷阱：`JSON.stringify(content)` 会把内层引号转义成 `\"`，
于是 `/"id":9/` 永远不匹配 —— **测试全绿但什么也没测**。

---

### A8. 别把"任务太难"当成"测不出差异"的解释（2026-09-12）

第一次 M3 对照（同 seed 各 3 局）得到"资金无差异"。
我的第一反应是"任务太简单/地图太小"，并建议"换更难的任务"。

**项目所有者追问了一句"差距不该这么小"，才逼出真因**：
**agent 根本没有决策可做** ——
- `observe()` 只返回 `date` + `companies`，**看不到城镇**
- 工具描述写着 *"Omit from_town/to_town to let the planner pick the best pair"*
  —— 框架在明说"你不用选"
- 决策其实发生在 GS 的 `PickTownPair()` 里

**实测证伪了"地图大小"假设**：地图放大 16 倍（256²→1024²），
仍然挑同一个 tile，站数仍是 2 —— 变大的只有 harness 自己的施工难度。

**规则**：A/B 测不出差异时，**先问"这个实验里，被测对象到底做了什么决策？"**
如果没有决策，任何指标都会饱和，加样本量毫无用处。
"任务太难"和"没有决策"是**两种**饱和，别混为一谈。

### A9. 修「可观测性」时，要问它会不会改变控制流（2026-09-12）

为了让心跳能被观测到，我给它加了自增计数器（§10.27）。
但 runner 判断"阶段变了没有"用的是**全字符串比较**，于是每一拍心跳都成了
"阶段变化"，**每次心跳唤醒一次 LLM 决策**。

实测后果（212 次决策）：93% 由心跳触发、83% 的决策间隔里 money 一分未动、
87% 的工具调用是 `observe`、**218 次重复同一句 plan**、
单局最多 5 次重复下发同一条指令。

**规则**：
- 存活信号按定义就是"什么都没发生"。**别把它接到"有变化就唤醒"的通道上。**
- 往一条**作为数据通道使用的字符串**（这里是公司名）里加字段之前，
  先确认**所有读者**怎么用它 —— 有人可能在做**等值比较**。
- 加了自增/时间戳这类"永远在变"的字段后，**任何 == 比较都会永远为假**。
- 观测（observability）与控制（control）**共用一条通路时，改观测就是改控制**。

---

## D. 环境与工具

### D27. 先用尽手里的库，再自己造（2026-09-18）

**现象**：为了"持久经验 + 自评"，我准备自己设计一套注入/校验/检索机制；
回头读 `pi-agent-core` 的 README 才发现，现成的钩子我们**一个都没用上**：
`beforeToolCall`/`afterToolCall` 里的"拒绝 + 理由会回到模型"、
工具级 `executionMode`、`sessionId` 的 provider 缓存、`thinkingBudgets`、
`steer()`、声明合并的自定义消息类型。

**更糟的是漏用带来了缺陷**：工具执行模式**从未设置 → 库默认 `parallel`**，
而游戏按 **FIFO** 应用命令（§10.39.1）——两个变更工具并发时，
**台账记录顺序 ≠ 实际应用顺序**，归因链条断了，而我们自己写契约时还以为它是 FIFO。

**规则**：动手设计新机制前，先把依赖库的**能力清单**与**默认值**读一遍，
并逐条问"我们用了没有 / 默认值对领域是否安全"。
**依赖库的默认值不是契约**——尤其当领域有不变量（顺序、幂等、原子性）时，
必须在自己的代码里显式声明。

**推论**：adapter 层的"我们自己的循环"会不断长出库已经提供的能力；
每轮改造前先做一次**能力对照审计**（本次结果写入 SPEC §10.74）。

**自我修正（同日）**：审计表里"未设 `sessionId` → 无缓存"这条是**我猜的**，
R1 实测证明缓存本来就发生（37–44% 命中率，SPEC §10.75）。
写"影响"列时必须区分**已测事实**与**推测**——否则审计本身就成了新的错误来源。

### D34. 源码里有这个分支 ≠ 它在可达状态下会被调用（2026-09-19）

**现象**：读 `CheckAddVehicles` 看到 `cur > want` 时**卖车**的完整实现，我据此断言
"环境本来就支持缩编，只是没被暴露"，并据此改了工具契约。真机探针（同一个杠杆、反方向）：
扩容 3→15 成功，**缩编 15→4 完全没发生**，请求之后再无 `fleet` 相位，结局时执行器仍在修路
——它此后再没回到过 `done`。

**根因**：那段代码的**调用点有守卫**：
`} else if (this._stage == "done" && this._vehicle >= 0) { this.CheckAddVehicles(); }`。
我读了**分支内部**，没有读**谁在什么条件下调它**。于是"能力存在"这句话在
"可达状态"这一层是假的：施工期占一局的大部分时间，请求等于永不生效。

**规则**：
1. 判断"环境是否支持某动作"，要沿着**调用链**往上读到**调用点的守卫**，
   而不是只读"动作的实现"。
2. 读源码得出的结论只是**待验证假设**，必须用探针在真机上量（本例正是探针挽救的）。
3. 探针自身也要按**被验证对象的时序**设计：我第一版只等 60 秒就去判定"不支持"，
   差点写出错误结论——正确的探针先等执行器进入它需要的阶段（`done`）再发请求。

**推论（对 D20 的补充）**：当年"同一个 `count:15` 重发 8 次、车队始终 6"，
除了"沉默的跳过"，还有一层结构性原因：**请求根本没被读到**。

### D33. 无法诊断的失败会被读成"没有内容"；两种成因不能混算（2026-09-19）

**现象（两段）**：
1. M1 开工时，同配置下偶发"某局 0 条 lesson"，可查的唯一记录是 `0 lesson(s) kept`——
   **看不出模型到底说了什么**，诊断卡住，只能猜。根因：反思 Agent 的消息只进 console，
   而 console 在 `/tmp` 里。
2. 修好可诊断性后第一次跑批，verdict 打出 `zero-tool-call runs 1` 并警告"协议/接线信号"——
   那一局其实是 **provider 失败**（`ok:false, toolCalls:0`）。**基础设施故障被读成提示词问题。**

**规则**：
1. 会产出结论的路径必须留下**当时到底发生了什么**的持久证据（审计，不是 console）；
   证据要包含"模型做了什么"（调了哪些工具、被拒几次、回复预览）而不只是计数。
2. 同一句"没有产出"下往往藏着**多种成因**，必须**分别计数、分别告警**：
   协议/接线问题 ≠ 基础设施故障 ≠ 确实没有内容。混算会让排查走向错误的方向。
3. 判据落地前先问："这两个数会不会被同一句话解释？"会，就还没分开。

**附带设计（值得复用）**：让人做对事，比逼人不做错事更便宜——
重试消息列出 **harness 已知的事实**，于是"如实记录"比"编造"更省力；
同时明说"仍无可支撑就什么都别记"，并把重试次数记账、在 verdict 披露。



### D32. "能不能做某件事"要沿**环境暴露了多少动作**这条链去查（2026-09-19）

**现象**：被问"接上 LLM 后能不能自主玩 OpenTTD"。我第一反应是从 agent 循环侧找答案
（循环写得好不好、调度器何时唤醒、上下文够不够）。真去查才发现答案在**另一头**：
GS 只暴露 **7 条命令**，其中变更型只有 `build_bus_route` / `add_vehicles`（外加清场），
所以 agent 的**玩法上限**是"建公交线 + 加车"——**与循环质量无关**。

**规则**：判断"agent 能否做 X"，要把链路**从环境往外**走一遍：
**环境暴露了哪些动作 → 其中哪些被包成工具 → 工具能否观测自身效果 → agent 是否有足够
决策次数去行使它们**。四层里任何一层是瓶颈，优化其它三层都不会让 X 变成可能。
**先量"动作面有多大"（命令数、变更型命令数、每局可行使次数），再谈智能。**

**推论**：这也是"记忆无处兑现"的结构性原因——可行使的**有后果决策**只有开头 1–3 个
（D17/D23）。**动作面与决策密度是一等指标**，不是外围。

### D31. 改协议时，提示词与运行时是同一份契约的两半（2026-09-19）

**现象**：R2b 把反思从"输出 JSON 再由我们解析"改成"调用 `record_lesson` 工具"，
代码、测试、门禁全绿。真机一跑：**两局都 `0 lesson(s) kept`**，且**零次拒绝**。
日志读起来像"这局没什么可学的"。

**根因**：**提示词没改**。它仍在说 `Reply with JSON only, no prose`，
于是模型规规矩矩回 JSON，一个工具都没调。运行时期待工具、提示词要求文本，
两半不一致——而失败**完全静默**：没有拒绝（没有调用就没有拒绝）、没有错误、
没有警告。

**规则**：
1. 协议是**提示词 + 运行时**两半的合体。改运行时必须同时改提示词，反之亦然。
2. 这种不一致**必须有交叉守卫测试**：提示词点名的每个工具都要真实存在；
   提示词不得再教已经废弃的协议；提示词必须说明"被拒会给理由"。
3. 需要区分"没有产出"的**两种成因**：模型没调用任何工具（接线/协议问题）
   ≠ 这局确实没东西可记。为此要报出**工具调用次数**，并在为 0 时 WARNING。
   这与 D28（披露要挂在真正的判决路径上）是同一条纪律的另一面。

**推论**：任何"让模型按新方式产出"的改造，都要在真机上看一次**产出是否真的来了**；
绿灯的单测只能证明"新路能走通"，不能证明"模型被引导到新路上"。

### D29. 用"追加"表达删除时，合并规则必须让删除赢（2026-09-18）

**现象**：R2 让经验可以被推翻：作废的实现是**追加一条同 id、带 `supersededBy`
的记录**（append-only 日志的常规做法）。写完测试却是红的——被作废的那条依然
以"未作废"的样子出现在库里。

**根因**：`dedupeLessons` 按"更可信（confidence）→ 更新（createdAt）"择优，
而那条作废记录与原条**这两个字段完全相同**（它是原条的副本），于是稳定排序下
**先到的原条赢**——删除被静默丢弃，记忆又变回只增不减。

**规则**：当"删除/作废"是以**追加**表达时，任何"合并/去重/择优"的规则都必须
显式让删除赢（它表达的是关于这条记录的**最新事实**），并且要有一条**反序输入**
的测试（先给作废记录、再给原条，也必须成立）。

**推论**：append-only 存储里的"状态变更"不是新概念，而是**新的合并优先级**——
写它的时候就该问一句"当两条同 id 的记录相遇，谁赢？我写的规则有没有覆盖这个新状态？"

### D30. 启发式守卫要用真数据量召回率，并写明它只是第二道闸（2026-09-18）

**现象**：R2 的内容守卫 `isImperative` 用来拦"祈使句/建议"（框架只给事实，不给策略）。
我在真机库里实测召回率：**53 条真实旧经验里只认出 19 条（36%）**；
按真机措辞（plan / front-load / defer / order / cluster / chain…）扩充动词表后到 **60%**。

**规则**：
1. 启发式守卫**必须**在真实数据上报出召回率，并写进文档——不然"我们加了守卫"
   会让人以为边界被保证了（实际漏了 40%）。
2. 结构性约束（"没有实测读数就不算经验"）才是主闸；启发式只能是**第二道闸**。
   能用结构判的就不要用措辞判。
3. 动词表要**来自真实样本**，不要凭空写（本项目对 fixture 的既有要求同样适用于词表）。
4. 同时要防误杀：给"观察句"的正例测试必须一直在。

### D28. 披露要写在"产生判决的那条路径"上，而且要验证它真的取到了数（2026-09-18）

**现象**：SPEC §10.66 写着"A/B verdict 会在任何收益主张之前打印通道健康
（`GS errors/run`）"。R1 做真机冒烟时顺手 `grep -rh "channel health" /tmp/*.log`
想引用它——**0 处**。这条披露从未在任何一个实验日志里出现过。

**两层原因**（都不是"忘了写"）：
1. 打印它的脚本 **不是跑实验的那个**：`m3-verdict.ts` 有这行，
   而每轮实验的摘要是 `run-experiment.ts` 打的（它当年没有这行）。
2. 即使有人跑 `m3-verdict.ts`，它也永远显示 `not reported` —— 它把
   `ArmSummary` 对象当作账本行去取 `gsErrors`，取到 `undefined` 就被过滤掉了。

于是"已披露"这句话在**实现上**是假的，而所有的文档都写着它是真的。

**规则**：
- 一个披露必须挂在**实际产出判决的路径**上（谁打印结论，谁打印它的健康度），
  而不是挂在"供人手动运行的分析脚本"里就算完成；
- 披露要有**自己的测试**：给一行带 `gsErrors: 5` 的账本，断言它真的打印出 `max=5`；
- 与 D26 的区别：D26 是"守卫检查的是包装"，本条是"披露根本不在路径上"——
  两者都是**只验证了形式、没验证效果**。

**从此衍生的做法**：聚合与缺省语义只有一处实现（`metricStat`/`degradationStats`），
脚本只负责打印；`reported` 与 `total` 分开报，**缺报 ≠ 0**。

### D25. 从哪个信号蒸馏经验，就等于在教什么（2026-09-18）

**现象**：反思（局终蒸馏 lessons）的 outcome 只有 money / vehicles / stations，
而实验用的判据是 `deliveredRun`（运货量）。于是**经验从被施工花费与贷款主导的
money 里蒸馏**，却用另一个量去评判结果。

**规则**：**蒸馏经验的输入必须与评判结果的判据同源**。否则 agent 学到的是
"怎么能少花钱"而实验在量"运了多少货"——两者都自洽，却互不相干。
这与"目标 ≠ 判据"是同一类错误（提示词说 profitable、判据用 delivered）。

**推论**：每加/每个判据换代，都要回头问一句"**反思看到这个新判据了吗**"；
本次 P1 修的就是这条。

### D26. 守卫包住"包装"却漏掉"内容"（2026-09-18）

**现象**：项目有一条硬边界——记忆只能注入**对过去的陈述**，不能是策略
（SPEC §10.22）。测试也确实"守"着它：断言注入行必须含 `previously`、且不得以
`DO|AVOID` 开头。

**真相**：注入行的形式是 `Previously an action like this paid off: <lesson>`，
于是 `^DO\b` **永远不可能匹配**（前缀在前）——**守卫只在检查自己写死的包装**。
真机库里 12 条有 10 条、27 条有 15 条是 `kind:"do"` 的**祈使句**
（"Build a single bus route first…"、"expand the fleet to around 16 vehicles"），
全都通过了守卫。更糟的是包装**替模型断言了因果**（"this paid off"），
而那条经验从未被验证过。

**规则**：**当守卫检查的是"我们包装出来的形式"，它就只保护那个包装。**
守卫必须作用在**内容**上（被注入的 lesson 文本本身），并且要有一条
"故意注入一句祈使句，守卫必须拦下"的反例测试。判断方法：把最坏的内容塞进去，
看守卫会不会红——不会红，就说明它守的是别的东西。

### D24. 要测"决策质量"，就得先把"运气"从窗口里拿出去（2026-09-18）

**现象**：自由局里 `delivered` 同车队下从 0 到 527，而 agent 真正有后果的决策只有开头
1–3 个——测量窗的大部分被"施工能不能建完"占据。

**修法（S1/G4 预建场景）**：开局先用确定性蓝图把线路建好，**等到可运营才开测量窗**。
施工仍然发生（-5.5 min），但它**不进测量**；窗内剩下的全是管理决策
（加车/扩线/配货）——正是 S0 证明**有陡峭响应**的那种决策。

**规则**：当一个实验的结局主要由**被测者控制不了的偶然过程**决定时，
不要用统计去"扣除"它（我们也证明了窗口长度与它不相关，r=-0.15），
而要**改设计把它移出测量窗**。代价是场景需要准备时间，收益是**测量的是决策而不是运气**。

**配套纪律**：场景没准备好必须**如实标废**（`scenarioReason != ready` → 判定排除），
否则"没搭好台子的那一局"会被算成一次平庸的表现。

### D23. 有杠杆 ≠ 杠杆到处都灵（2026-09-18）

**现象**：S0 oracle 梯度显示车队规模确实影响结果（9→15 辆：+39% 交付），
但在 **15 辆附近塌陷**（15→21 辆只 +6%，边际从 +70 掉到 +15 交付/车）。

**规则**：在把某个变量当作"决策空间的杠杆"之前，先**测它的响应曲线形状**，
并确认实验的效应量落在**陡峭区间**。否则"处理组和对照组没差别"可能是
**饱和**造成的，而不是"记忆没用"——这会把一个设计问题误读成研究结论。

**同时**：三局的**原始当季读数**是 0 / 22 / 83，而积分量是 1088 / 1511 / 1598。
若沿用 D14 之前的判据，S0 会得出"车队规模毫无影响"甚至"什么都没运"。
**同一个 bug 会在每个新实验里重现一次**，直到它被修掉为止——这也是"先修仪器、
再谈结论"顺序的又一次验证。

### D22. 同一个事实有两条通道时，粒度不同（2026-09-18）

**现象**：G1 的边界"分辨率 ≈3 游戏日"是我**算出来的**（按 GS 200 tick 推断），
但实测 horizon 40 会记成 60、25 记成 30。核对 `DEFAULT_SUBSCRIBE` 才发现：
admin 的 `Date` 订阅频率是 **Monthly = 每 30 游戏日一次**，而 GS 的原始日期是每 200 tick。
**两条通道都存在，粒度差 10 倍，我用了粗的那条却没意识到。**

**规则**：同一个量若有**多条可取通道**，选之前先问"**它们的粒度/语义一样吗**"，
并把**选了哪个、为什么**写进注释与文档。用错粒度不会报错，只会让结果**悄悄变粗**
（本例：边界只能切到整月）。

**同时暴露三处"声明了但没接线"**（与 D19 同源）：`loop.ts` 只转发墙钟秒；
CLI 没把 `--game-days` 转发给 v02 路径；v02 的时钟只读 `world.date`（粗粒度）。
→ **每加一个契约，都要在每个执行路径上验证一次**（agent 路径通过 ≠ v02 路径通过）。

**顺带**：`--game-days` 的探针首次运行因"墙钟上限太小（900s < 施工+运营所需）"
整批作废——**给探针设上限时，要按"最慢的那条路径"估**，否则数据会被
`reachedHorizon=false` 全部排除。

### D21. "在动"不等于"在前进"——给 agent 的必须是推进，不是活动（2026-09-18）

**现象**：agent 在一局里连下 68/110/161/239/271 格的线路，每条都排在同一条 FIFO 后面，
并用 193 次工具调用轮询。它并非没看环境——它看的是**活动**（心跳、阶段在变），
而不是**推进**。

**根因**：harness 把"有动静"当作可观测性，但 `rd s0 r17 d115` 里 `r` 是**寻路搜索步数**
（每 tick 都在涨，看起来一切正常），**`d`（还剩多少格）才是推进**。没有 `d` 的序列，
agent 就无法回答"这条路来不来得及建好"——而那正是这一局的唯一约束。

**规则**：给 agent 的进度必须是**单调的、有终点的量**（剩余量、完成度、ETA），
不是"还在跑"。判断方法：问"这个数字能不能回答'还有多久'"；不能，它就只是活动。
**同理**：把活动当进度上报，会让 agent 以为系统健康——而它其实卡在一个它看不见的地方。

### D20. 沉默的跳过 = 让 agent 学会"白做"（2026-09-18）

**现象**：agent 反复请求车队 10/15（同一 `count:15, job:1` 出现 **8 次**），
而 6 局里 5 局最终车队仍是 6。

**根因**：executor 只对**当前 job** 应用车队请求，**跳过时一句话都不说**。
于是 agent 收不到任何"没生效"的信号 → 重试 → 再失败 → 白烧 token，
而它的记忆里学到的是"请求车队没用"这种**错误教训**。

**规则**：**任何"被忽略的输入"都必须变成一次明确的拒绝**。
沉默不是中性的：它教给 agent 的是错的因果（"我做了 X，什么都没发生"）。
这与本项目"工具必须能观测自身效果或明确拒绝"同源，但更容易被漏掉——
因为失败发生在**别人的分支**里（这里是 executor 的 job 过滤），
而不是在工具自己的代码路径上。

**副产物（两次自我纠正）**：① 我原以为 `_fleetApplied` 跨 job 泄漏，查证后发现
换 job 时会重置——**假设不成立**，但按线路记账的语义混用是真的（`want` 按线路、
`cur` 数全公司）。② 新阶段的第一次实现每 tick 都发，会**顶掉路网进度阶段**，
故加了按请求去重（阶段通道是主进度信号，不能被常驻抱怨淹没）。

### D19. 契约改了，**边界执行**也要跟着改（2026-09-18）

**现象**：把局改成"推进 N 游戏日后结束"（G1）后，第一版实现记录 `horizon 40 → simulatedDays 60`、
`horizon 25 → 30`。契约看起来对，实测**过冲 20%**。

**根因**：边界只在**两次决策之间**检查，而一次决策（模型调用）是**墙钟数十秒 ≈ 十几游戏日**，
于是每次都整段冲出边界；后来加的"决策期间每秒检查"也治不了本——**模型调用无法被打断**，
只能等它返回。真正的修法是**在边界到来前不再提问**（余量 = 上一次决策实测消耗的游戏日），
让世界空转到边界。

**规则**：改了一个**契约**（"局多长"），必须同时问"**这个边界在执行路径上能不能被守住**"，
并用真机**测量过冲**，而不是相信契约文本。**声明 ≠ 保证**；测量才是保证。

**副产物**：顺带发现 `loop.ts` 只把 `secondsRemaining` 转发给模型（新字段被静默丢弃）——
"新增事实"必须验证它**真的到达模型**，否则等于没加。这条与 C1（`toWireSnapshot` 两路径只改一条）
是同一个错误模式。

### D18. 先定义"一次观测"的边界，再谈结果（2026-09-18）

**现象**：同样的 `--demo-seconds 900`，六局分别推进 **449 / 445 / 273 / 445 / 0 / 448**
游戏日——**模拟机会相差近一倍，还有一局是 0**。而我此前把它们的 delivered 直接放一起平均。

**根因**：`--demo-seconds` 定义的是**墙钟**长度，**没有任何地方定义模拟长度**。
墙钟→模拟时间的换算率随 ① 机器负载（脚本/AI 吞吐）② **agent 自己调 `set_pause`**
（本轮 8–16 次）③ 冻结机制 而变。于是"同一个实验里的两局"是**长度不同的世界**。

**最危险的一条**：那局推进 **0 游戏日**的运行被记成 `deliveredRun=0`——
**"世界停住了"与"什么都没运"在记录里无法区分**。

**规则**：任何实验，**先写下"一次观测"的边界**——它持续多少**被测现象自己的时间单位**
（这里是游戏日），用什么保证每次观测的边界一致，以及边界之外的观测如何标注。
墙钟预算是**工程约束**（要能在合理时间内跑完），**不是测量定义**。
推论：时钟必须以**被策量的单位**呈现给被测者——给 agent 墙钟秒去推理
"这条路来不来得及建好"是把时间说错了语言。

**（附带纠正 D17 的一处过度解读）**：D17 里我写"`date=1950-01-01` 始终不走"，
那只是前两次决策附近的采样，证据不足；真正的证据是上面这张表。

### D17. 决定结局的那个约束，必须是被测者能看见的事实（2026-09-18）

**现象**：一局用了 193 次工具调用（占该臂 token 82%），日志里 `date=1950-01-01`
**始终没走**——agent 在等 executor 修一条 115 格的路，只能反复 observe/inspect 空转；
同时它还在连下 68→110→161→239→271 格的新线路，而这些都排在同一条 FIFO 队列后面。

**根因**：harness 给了钱、城镇、线路经济、阶段解码，**唯独没给施工进度/队列/吞吐/ETA**；
`estimate_route` 只给"格数+成本"，**不给时间**。于是 agent 无法回答"这条线来得及建好吗"，
只能轮询——而**时间与施工吞吐正是这一局的唯一约束**。

**规则**：给 agent 的事实清单里，**必须包含"当前真正的瓶颈资源"的状态**。
判断方法：问"如果这一局失败，最可能卡在哪？"——那个东西的状态就**必须**在上下文里
（进度、队列、吞吐、等待时间）。否则模型只能在**盲区**里做决策，再用轮询把盲区填满
token。这与"harness 给事实不给策略"不冲突：**吞吐与队列是事实**，不是建议。

**副产物**：这也解释了"记忆无处兑现"——一局里真正有后果的决策只有 1–3 个，
且都在开头，运营期几乎没开始。**决策数（尤其是"有后果的决策数/局"）应当作为一等指标。**

### D16. 修好仪器 ≠ 降低方差（2026-09-18）

**现象**：我连续找到两个真实仪器缺陷（当季计数器 §10.65、部分失效通道 §10.66），
修完自然预期"方差该小了"。重测（3+3 局）：**CV 仍是 1.25**，同臂内从 0 到 225。

**根因**：我把"**测量错了**"与"**波动大**"当成了同一件事。仪器缺陷让数字**不可比**，
但方差来自**被测对象**：这局建成了没有、做了 5 次还是 12 次决策、选了哪条线。

**规则**：修仪器与降方差是**两件独立的工程**，必须分别验证——
修完仪器后要**重新测 CV**，不能假定它变小。并且：**"不可比"与"噪声大"要分开报告**，
否则会把"我们量错了"当成为什么结论出不来的解释，从而**掩盖真正的方差来源**。

**副产物**：重测暴露出更具体的方差来源——一局工具调用 **193 次/11 决策**
（正常 2–3 次/决策），单局占该臂 token 总量 82%，把 tok/dec 抬到 9 倍。
**逐局工具数当前无上限**，这既是噪声也是可干预点。

### D15. 部分失效比彻底崩溃更危险（2026-09-18）

**现象**：两轮 A/B 方差大到无解、方向还相反；我一直在找"任务/判据"的原因。

**真相**：GS 的 `route_stats` 通道**部分失效**了两轮——`GSStation.IsStationTile`
这个不存在的函数在"挪址兜底"的半径扫描里，**只**让这一种请求报错，其余通道一切正常。
17 局**全部**命中，但单局从 2 次到 152 次不等：有的局只丢 1% 读数，有的局丢 92%。
`ctl4`（那局 delivered=0）只拿到 20 条读数 = agent 在近乎**失明**状态下跑完全程。

**规则**：
1. **部分失效不会自己暴露**。崩溃会留下堆栈、退出码、日志；部分失效留下的是
   "看起来正常的运行"。凡是"某个请求可能单独失败"的通道，**必须逐局计数并放进结果旁边**。
2. **"每次调用都成功"≠"这条通道可靠"**：该调用在**条件分支**里（只在挪址时走），
   所以我此前几十次真机运行都没见过它——**没出事的分支不等于没坏**。
3. 新增 `GameMetric.gsErrors` + verdict 的 `channel health` 行：把"通道是否健康"与
   "结果是多少"**印在一起**。历史行显示 `not reported`，不假装干净。

**为什么静态守卫值得**：Squirrel 只能在 OpenTTD 里跑，单测抓不到错误 API 名。
守卫用**证据黑名单**（条目必须来自线上观测到的 `reason` 字符串），并同时断言
"半径扫描仍存在"——否则删掉扫描也能消错，代价是把挪址后的线路静默报成"无车辆"。

### D14. 会重置的计数器不是测量值（2026-09-18）

**现象**：连续两轮 A/B 得出**相反方向**（校准轮 treatment 领先 306 vs 210，
block #1 control 领先 190 vs 137），方差大到任何结论都立不住。

**真相**：判据 `delivered` 直接读 `economy.deliveredCargo`，而它是 **OpenTTD 的
当季计数器（每季归零）**。于是"运行结束那一刻的读数"测的是**随机一段部分季度**：
恰在季界后结束的局记 0（`ctl4` **建成却 0 交付**），其余局各覆盖 0–90 游戏天不等。
季度相位 + 施工时序 = 我看到的全部"对方差无解"。

**规则**：一个**会重置/会滚动**的计数器，**不是**一个可直接比较的观测量。
用它之前先问三句：① 它的窗口是什么（季/年/会话）？② 窗口边界与我的观测窗口对齐吗？
③ 不对齐时能不能**积分**成全程量？——我先前只问了"这个字段叫什么"（"deliveredCargo
听起来是累计的"），这正是 §2 铁律"协议有事实依据"最容易破的一处：**字段名不是定义**。

**代价可回收**：修法是 `src/game/delivery-meter.ts`（跨季积分，含缺口诚实标记），
判定改成两臂同源（`deliveredSource`）。教训落在"先核对字段语义，再拿它当判据"。

### D13. 退出码不是错误码——先读它由谁写（2026-09-18）

**现象**：block #1 结果里 treatment 有一局 `rc=1`，我立刻按"进程崩溃 → 那局作废 →
`built=0.80` 是崩溃产物"的思路去查日志。

**真相**：`src/agent/reflect-run.ts:162` 是 `return reachedDone ? 0 : 1` ——
**rc=1 = 施工未达 DONE**，是框架的诚实自报（该局日志里 `constructionDone=false
phase="EX stA_wait"` 完全吻合），数据有效、`built=0.80` 是真实结果。
日志里也没有任何堆栈，唯一的 `ELIFECYCLE` 来自 pnpm。

**规则**：不要把"非 0 退出码"直接等同于"基础设施故障"。**先 grep 出写这个码的地方**
（`return .*? 0 : 1` / `process.exit`），再决定那局是废是留。差点把一局正常实验判废，
也差点在报告里写下"我们的 harness 崩了一局"这种不实陈述。

**顺带**：`run-experiment.ts` 打印 `rc=${code}` 而不解释含义，是**输出方**的缺陷；
可读的输出应自解释（rc≠0 时附原因）。

### D12. 实验进行中不得改 `src/`——被测对象必须整个 block 冻结（2026-09-18）

**现象**：等待 block 期间想"顺手"做 NEXT-6 卫生项（删 `v02-runner`、分诊裸 `catch {}`）。

**根因**：`scripts/run-experiment.ts` **每局都重新 spawn** `pnpm exec tsx src/cli/run.ts`，
即 tsx **从磁盘现读源码**。block 中途改 `src/` ⇒ 同一 block 内前几局跑旧代码、
后几局跑新代码，两臂之间与局之间**同时**被污染，而 verdict 完全看不出来。

**规则**：真机 block 运行期间——
- **禁止**修改 `src/`、`scripts/run-experiment.ts`（= 被测对象与其驱动器）；
- **允许**改文档（SPEC/CHANGELOG/ROADMAP/MEMORY/README）与纯分析脚本；
- **禁止**跑重测试套件（wall-clock 预算的实验会被 CPU 争抢拖慢，等于偷走 game-time）；
- 要改代码，**先等 block 结束或先停掉它**（并如实说明这个 block 作废）。

比 D5（实验期间不跑 gate）更强：D5 只防止**假红**，D12 防止**真污染**。

### D11. 先怀疑实验设计，再怀疑任务难度（2026-09-18）

**现象**：连续多轮 A/B 都"不可判定"，我把它归因为"任务太浅、记忆没有出题空间"。

**真相**：**500s 的窗口把方差塞进了结果**。把窗口提到 900s：零交付率 70%→**0%**、
建成率→**100%**、CV **1.95→0.48**、力量对同一效应从 0.08 升到 0.34（+46% 效应时
0.83 需 25/臂）。任务的"浅"没变，变的是**测量设计**。

**规则**：结果不可判定时，先问三个设计问题——① 每个观测是否被**截断**（窗口/超时）；
② 结局是否**零膨胀**（大量观测根本没到达可测量状态）；③ 方差是否来自**未控制的
场景差异**（路线长度/起点）。修设计通常比加样本便宜一两个数量级。

**推论**：`n` 永远由**事先声明的 MDE + 实测分布**反推，不由"别人都跑 30 局"决定。

### D10. 重尾判据上，均值可以给出与中位数相反的结论（2026-09-17）

**现象**：NEXT-2 收尾 A/B（n=5/臂）delivered 逐局 with memory `[0,77,39,0,0]`、
without `[0,23,0,18,15]`——**均值 favor 记忆（23.2 vs 11.2），中位数 favor 对照
（0 vs 15）**，符号相反。总和 116 里有 77 来自同一局。

**根因**：delivered 是**零膨胀重尾**量（60% 的局为 0，一旦跑通就很大）。在 n=5 上
比较均值 = 让最尾部那一局代表整臂。**这也解释了前三轮（§10.52）为什么总"不可判定"**
——那时被判为"任务太浅"，其实还有一个统计层的错：**用均值比较重尾量**。

**规则**：比较前先看分布。零膨胀/重尾量必须同时报告**中位数与零率**（或自助法区间），
均值与中位数符号不一致时**不许下结论**。另：**判据守卫必须按量定制**——`builtRate`
守卫是给 money/income 的（"没建成→没花钱→钱更多"），对 delivered 它恰好反号
（没建成→没运货→0 是诚实结果），拿它挡 delivered 就是用别的量的病否决本量的问题。

### D9. 暂停的代价是"世界停止推进"（2026-09-17）

**现象**：冻结 A/B 中 frozen 臂 delivered 0/5，unfrozen 2/5；同时 frozen 局少走约
10% 游戏天数。

**根因**：暂停不只是"冻结了输入"，它**冻结了世界本身**——executor/GS 的脚本 tick
停摆，施工在模型思考期间完全停住。本任务的瓶颈恰是施工的墙钟时长，于是冻结把
"思考"与"施工"从并行改成串行。

**规则**：引入暂停前先问**瓶颈在哪**。若瓶颈是"世界自己在推进"（施工/生产/等待），
暂停是净亏；若瓶颈是"决策必须落在见过的状态上"（高频、短动作），暂停才值。
且比较必须按**游戏时间**对齐——墙钟对齐会系统性惩罚暂停的一方。

### D8. 继承来的"事实"必须重测（2026-09-17）

**现象**：SPEC §10.28 写"rcon pause 是单向的（永久冻结）"，代码注释与静态守卫都
沿用这个信念整整五天。用新做的 rcon 回执通道 + `getdate` 实测：
**pause → 日期冻结 → unpause → 日期恢复推进**，双向都有效。

**根因**：当时的失败有**另一个**原因（`pause_on_join=true` 且无客户端），
却被归因到 rcon 本身，写进 SPEC 成了"事实"；后续所有代码与守卫都在这个
**未被复核的结论**上继续盖楼。

**规则**：把结论写进 SPEC 时，**必须写下当时的观测条件**（配置/版本/命令）；
条件变了要重测。凡"某 API 不可用/只能单向"这类**否定性结论**，尤其危险——
它们会长期挡住正确的设计（这里挡住了"冻结世界"这一选项）。

### D7. 实验变量必须在**分配时**记录，不能事后推断（2026-09-17）

**现象**：/tmp/n2ab 的 A/B 自报"5 vs 5"，逐局核对实为 **4 vs 6**。新 dataDir 的
第一局 treatment 因"没有任何记忆可注入"（lessons=0, facts=0）被算进了 control。

**根因**：臂划分用「注入了多少」而非「分配到哪一臂」。这是 m3f 缺陷（"treatment
只看 lessons"）的下一层——上次修了"facts 也算注入"，这次暴露"**注入为空 ≠ 不是
treatment 臂**"。

**规则**：**任何实验自变量都必须在分配时写进记录**（这里是 `arm`），事后由观测值
推断归类一定会漏——尤其"干预没送到"这种局，它看起来和对照组一模一样。
推论：还要**显式统计并点名**"分到 treatment 但干预为空"的局数。

### D5. 真机实验期间不得跑 gate（2026-09-16）

**现象**：n=5 实验运行中跑 `pnpm run gate`，preflight 三个单测失败（断言端口空闲）
——实验正占用 3977/3979。

**根因**：单测默认端口空闲，真机实验打破该前提。**不是代码回归**，但红门禁会让人误判。

**规则**：真机实验运行期间只看目标日志，**不跑 gate**；实验结束后再验。
一条真机实验与一个绿门禁不可同时拥有。

### D4. 实验缺陷与代码缺陷同源：注入/计分链路必须端到端自证（2026-09-15）

**现象**：M3 第四次重跑连废两轮——done 相位匹配失败使"完成"永不落地；
臂计分漏了 route facts；`--no-memory` 没关 facts 注入（控制臂被注入，
实验失去对照）。跑局本身是正常的，废掉实验的是**注入与计分链路的三个洞**。

**根因**：每个洞单独看都"不影响运行"——游戏照跑、账本照写、判定照出。
它们的共同作用是**让两臂不再是可以比较的两臂**，而这恰恰是实验的全部意义。

**规则**：
1. 加一条注入通道时，必须同时回答三问：**它被写进实验账本了吗？它被
   --no-memory 关掉了吗？它出现在对照臂判据里了吗？**（三者缺一即判废实验）
2. 判定脚本的"不可比"守卫（confounded）是资产不是障碍——它这次正确拒绝
   了一个 −8,355 的假结论。
3. 一轮真机实验的产出不只有数字：**判废轮里修掉的三个缺陷，比一个漂亮的
   数字更值钱**。


### D0. 环境事实只有一处权威（2026-09-12 起，2026-09-19 收缩）

**规则**：凭证路径、OpenTTD 二进制、默认 dataDir、门禁构成、remote、subagent 可用性、
单局耗时——**唯一权威是 `ROADMAP.md` 顶部交接块 §3「恢复工作所需的环境事实」**。
本节只留一句提醒：**跑真机实验前把 `credentials.json`+`llm.json` 拷进实验 dataDir**，
否则门禁以 `llmConfigured` 拒绝启动（本条属于"易忘的前提"，值得在这里复述）。

### D1. 清理开发进程必须用对的匹配模式（2026-09-12，导致门禁假红）

**现象**：`pnpm run gate` 以 3 个 `preflight.test.ts` 失败告终，看起来像代码回归；
真因是残留 `--serve` 进程占着端口，而我的清理命令 `pkill -f "cli --"` **匹配不到**
真实命令行 `src/cli/run.ts --serve`，于是我以为清干净了，其实没有。

**规则**：清理命令与复核方式见 `ROADMAP.md` 顶部交接块（唯一权威）。
要点：模式要写**真实命令行里存在的子串**（`cli/run.ts`、`OpenTTD.app/.../openttd`），
清完用 `lsof -nP -iTCP:<port> -sTCP:LISTEN` **复核**；
**`pkill -f openttd` 会误杀**无关进程；跑 dev/测试前确认 `OPENTTD_DATA_DIR`
指向隔离目录（AGENTS §2 铁律 3）。

### D2. subagent 不可用不要静默降级（环境事实，重复验证 5+ 次）

**现象**：本环境缺少 `@earendil-works/pi-server`，异步/后台 subagent 委派**不可能**。

**规则**：委派失败时**显式报告偏差**，不要静默改为全内联执行后假装按计划走了。
本仓库实际全部工作都是内联完成的。

### D3. 静态资源没有任何缓存校验 → "刷新"救不了你（2026-09-12）

**现象**：修好了 bug、让用户刷新，用户看到的**还是旧的**。
排查时我也一度怀疑"用户没测到新版"。

**根因**：`server.ts` 的静态处理只写了 `content-type`，**没有 `Cache-Control`、
没有 `ETag`、没有 `Last-Modified`**。无校验信息的响应允许浏览器启发式复用，
于是"普通刷新"可能继续跑旧 JS。

**规则**：这个仓库**没有构建链**，JS 每个提交都在变，所以静态资源必须
`Cache-Control: no-cache` + `ETag`（每次重新校验，未变返回 304）。已实现。

**教训**：当"我这边是好的"与"用户那边是坏的"同时成立时，
先怀疑**版本不一致**（缓存/进程/端口），再怀疑逻辑。排查时要问：
**用户跑的是我改的那份代码吗？**

---

## E. 提交卫生

### E1. 阶段收尾漏同步文档（2026-09-15，项目所有者发现）

**现象**：Phase A/B 重构全程只更新了 SPEC + MEMORY，CHANGELOG 与 ROADMAP
的待办区停留在重构前状态（NEXT-1/1b/1c/2b 实际已完成却仍标"当前要做"），
直到所有者质问"为什么没有每阶段完成后同步修订 CHANGELOG、ROADMAP、MEMORY"才补。

**根因**：重构期间节奏是"实现→gate→commit"，把 DoD 的文档同步当成了
"收尾时再做"，而阶段被拆成了多个 commit——每个 commit 都觉得"还没到收尾"。

**规则**：
1. **每个 commit 就是一次收尾**：commit 前自问"CHANGELOG/ROADMAP/MEMORY/SPEC
   是否都反映此刻现实？"——任一为否就补在同一个 commit 里。
2. 阶段完成时额外跑一遍 **DoD 文档清单**（AGENTS §7）：CHANGELOG（改了什么）/
   ROADMAP（状态与下一步）/ MEMORY（教训 + §0 状态列）/ SPEC（事实）。
3. ROADMAP 的"待办"极易腐烂——已完成项必须当场划掉并写指针，不许留到下轮。


### E2. 同一次工作的收尾不要拆成多个 commit（2026-09-12，用户指出）

**现象**：一次「把待办写入 ROADMAP」的文档工作产出 **3 个 commit**：
记录待办 → 补一个环境陷阱 → 修正自己写错的 VCS 说明。
后两次都是**同一次更新的收尾**，本应 `--amend` 进第一次。

**根因**：把"每次工具调用成功就提交一次"当成了习惯，而没有先问
「这属于同一次逻辑变更吗」。

**规则**：见 `AGENTS.md` §8。判定标准：**「如果我在写第一版时就知道这些，还会单独提交吗？」**
答"不会" → `git commit --amend`（有 remote 后：**仅对未 push 的提交**安全）。

---

## F. 文档一致性

### F1. 同一事实散落在多个文档 = 必然不一致（2026-09-12，用户指出）

**现象**：`ROADMAP.md` 末尾有一份「开发纪律」5 条，与 `AGENTS.md` §2 铁律**重复表述**；
`ROADMAP.md` 顶部的进度速览还停留在旧版本，完全没有反映 v0.6.0 做的事。
另：我把"26 个 commit 未 push"写进 ROADMAP——**当时这个仓库根本没有 remote**（2026-09-17 起有），
错误结论被白纸黑字记下。

**根因**：写文档时"就近复制"而不是"指向权威位置"；没有人核对数字/结论。

**规则**：见 `AGENTS.md` §9 文档职责表。**改一个事实需要动几个文件？> 1 就是有重复。**
写进文档的**具体数字与结论必须当场核实**（如 `git remote -v`），不要凭印象。

---

## 附：快速自检清单（开工前 / 交接前）

- [ ] 这次改动**真的跑过了**吗？跑在**有数据的状态**下吗？（A1）
- [ ] 断言是**用户能观察到的结果**，还是代理指标？（A2）
- [ ] 涉及 UI：交互元素**逐个点过**了吗？console 的 **warn** 也看了吗？（B1、A1）
- [ ] 有没有**两份实现**同一个概念？（C1）
- [ ] 是否在改一个有**实测理由**支撑的决定？理由读了吗？（C2）
- [ ] 端口/进程清干净了吗？`OPENTTD_DATA_DIR` 设了吗？（D1）
- [ ] 这次是不是该 `--amend`？（E1）
- [ ] 文档**一次性同步全**了吗？有没有重复表述？（F1、AGENTS §9）
